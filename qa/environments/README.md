# Environments QA

This surface owns environment provisioning and lifecycle behavior.

Owned areas:

- environment creation and reuse
- lifecycle transitions
- resume and attach behavior
- worktree and shared-environment expectations

Default pass:

- [`./core.md`](./core.md)

Fastest scripted entrypoint:

- `pnpm qa:environments:core`
- `pnpm qa:environments:attachments`
