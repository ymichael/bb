# Orchestrator + Task Model (Split)

This plan has been split into two implementation tracks so they can be worked independently:

1. **Task model**: `plans/task-model.md`
2. **Orchestrator mode**: `plans/orchestrator-mode.md`

Recommended order:
- Ship task model first.
- Build orchestrator mode on top of that foundation.
